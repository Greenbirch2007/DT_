

基础知识


数据类型

标准库



字符串，元组，列表，字典，集合


一. 常用数据类型和数据结构
1.　整型的表示方式使用哪个关键字？　举例,写出一个整型的表达式？(赋值给a)
2. 浮点型的表示方式使用哪个关键字？　举例,写出一个浮点型的表达式？(赋值给a)
3. 元组的表示方式使用哪个关键字？　举例,写出一个元组的表达式？(赋值给a)
4.　列表的表示方式使用哪个关键字？　举例,写出一个列表的表达式？(赋值给a)
5.　字典的表示方式使用哪个关键字？　举例,写出一个字典的表达式？(赋值给a)
6. 字符串的表示方式使用哪个关键字？　举例,写出一个字符串的表达式？(赋值给a)
７. 布尔值的表示方式使用哪个关键字？　举例,写出一个布尔值的表达式？(赋值给a)

二．简单编程实践(可以在自己电脑上练习测试确认后写出最终结果)

1. 将两个字符串进行拼接,并输出出来．
a = "hello" 
b= "world"
输出a与b的拼接和数据的代码块
2. 引入标准库或第三方库的方式是什么样子？(提示：可能用到import,from,as,into，这４个关键字)，请分别引入一个标准库和一个第三方库(默认已经安装)(写出代码块)

3. 已知a = [1,2,3,4,5] ,请问
 第一问，如何处理可以得到　b= [3,4]
　第二问，如何能够得到c = [1]
 第三问，如何能够得到d = 1
 第四问，如何能够得到　e = [3,4,1]

4. 已知a = (1,2,3,4,5),请问
 第一问，如何处理可以得到　b= (3,4)
　第二问，如何能够得到c =1
 第三问，如何能够得到d = (3,4,1)

5.  已知字符串s = "   basdfaer     " ，请问：

第一问，如何可以字符串s前后的空格,请将剔除后的结果赋值给b,请问此时b是什么数据结构？

第二问，请直接将s = "   basdfaer     ",修改为ss = "basdfaer",写下代码片段

6.内置函数是Python自带的函数，直接在代码编程中就可以使用的．请写出5个Python的内置函数，并说明它们的作用
(提示，print()为了将程序输出结果打印出来)

7.在以下异常处理代码中，finanlly是什么含义？
try:
　pass
except:
	pass

finanly:
	pass
8. 请定义一个函数实现加法的功能(提示，比如直接输入1,2就可以得到3)
9. a= [1,2,3] ,要实现a=[1,2,3,4] 用到了list的什么方法？
10. a= [1,1,2,3,4,4,5,6,6,8],用什么数据结构可以实现 a= {1, 2, 3, 4, 5, 6, 8}?(提示,也就是剔除重复项)
11.用匿名函数(lambda表达式）实现一个乘法运算，给出2*3的结果并输出.

12. 正则表达式匹配中，（.*）和（.*?）匹配区别？







二．常用标准库(请写出以下标准库的主要作用(选择8个))
re,os,time,threading,Multiprocessing,asyncio,time.datetime,sys,doctest,tkinter,random,copy,socket,logging


三．常用第三方库(请写出以下第三方的主要作用(选择5个))

requests,pandas,BeautifulSoup,flask,django,sqlalchemy,selenium,PyQt5,lxml,pymysql,cx-Oracle,scrapy,pymongo,Scikit-learn,Tensorflow, Matplotlib
